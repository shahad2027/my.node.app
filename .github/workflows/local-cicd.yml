name: Localhost CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3

    - run: |
        docker stop my-node-app || true
        docker rm my-node-app || true

    - run: docker build -t my-node-app .

    - run: docker run -d -p 3000:3000 --name my-node-app my-node-app
